/** @type {import('next').NextConfig} */
const nextConfig = {
  // ⚠️ Note:
  // The "allowedDevOrigins" option was removed in Next.js 15+
  // If you need LAN testing, just open the "Network" URL shown in your terminal
  // (example: http://192.168.1.161:3000). No config needed.
  // experimental: {
  //   allowedDevOrigins: ["http://192.168.1.161:3000"],
  // },
};

module.exports = nextConfig;
